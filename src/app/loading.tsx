/**
 * Global loading convention. Real pages should prefer a skeleton matching
 * their final layout (DESIGN_SYSTEM.md §7 Loading States) over this
 * fallback — this exists so the route segment always has one.
 */
export default function Loading() {
  return null;
}
