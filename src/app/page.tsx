/**
 * Foundation placeholder — confirms fonts, Tailwind tokens, and the App
 * Router shell are wired correctly. The real homepage (PLANNING.md §8) is
 * built in a later phase, not here.
 */
export default function HomePage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-text-muted font-mono text-xs tracking-[0.1em] uppercase">
        HubZero — foundation
      </p>
    </main>
  );
}
